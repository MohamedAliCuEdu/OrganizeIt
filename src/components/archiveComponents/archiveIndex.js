import ArchiveEmpty from "./archiveEmpty";
import ArchiveHeader from "./archiveHeader";
import UnArchiveAllBtn from "./unArchiveAllBtn";

const ArchiveIndex = {
  ArchiveHeader,
  UnArchiveAllBtn,
  ArchiveEmpty,
};

export default ArchiveIndex;